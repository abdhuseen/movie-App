<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MovieApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/black</item>-->
    </style>

    <style name="Theme.MovieApp" parent="Base.Theme.MovieApp" />
    <style name="ToolbarMenuTextAppearance" parent="TextAppearance.AppCompat.Widget.ActionBar.Menu">
        <item name="android:textSize">25dp</item>
        <item name="android:textColor">@color/white</item> <!-- Change this to your desired color -->
    </style>
</resources>